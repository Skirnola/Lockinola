"use client";
import { useState } from "react";
import { ArrowRight, BookOpen, Check, Clock3, Cloud, Code2, GraduationCap, Headphones, Leaf, Pencil, Play, Plus, Terminal, Trash2, Video } from "lucide-react";
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";
import { Progress } from "@/components/ui/progress";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { createRecordId, type PlannedSession, type SessionKind, type TrackName, type Weekday } from "@/lib/learning-data";
import type { ProgressSnapshot, TrackProgress } from "@/lib/progress";

export type Lesson = { title: string; kind: string; detail: string; track?: string; week?: number; time?: string; sourceName?: string; sourceUrl?: string };
const paths = {
  Coding: { color: "coding", icon: Code2, label: "CODING · MONTH 01", headline: "Python foundations", description: "A four-week beginner route: watch only what you need, type the examples, then build something small.", current: "First-month route", stages: ["Variables & functions", "Conditionals", "Loops & collections", "Small command-line project"], credential: "Course option: CS50P OpenCourseWare. Its certificate is optional; skill comes first.", lessons: [
    { week: 1, title: "CS50P: functions and variables", kind: "Course lesson", time: "45 min", icon: Video, sourceName: "CS50P · Week 0", sourceUrl: "https://cs50.harvard.edu/python/weeks/0/", detail: "Watch the Functions and Variables lecture in chunks. Stop after variables, print, input, numbers, and strings. Type each example yourself and keep three notes: what goes in, what changes, and what prints." },
    { week: 1, title: "Build a tiny introduction program", kind: "Practice", time: "30 min", icon: Terminal, detail: "Create intro.py. Ask for a name and graduation year, then print one formatted sentence. Change both inputs twice and explain the output without running it first." },
    { week: 2, title: "CS50P: conditionals without guessing", kind: "Course lesson", time: "45 min", icon: Video, sourceName: "CS50P · Week 1", sourceUrl: "https://cs50.harvard.edu/python/weeks/1/", detail: "Study if, elif, else, Boolean expressions, and and/or. Since conditionals are familiar, focus on reading the path a value takes through the program." },
    { week: 2, title: "Make a grade classifier", kind: "Practice", time: "35 min", icon: Code2, detail: "Ask for a score from 0–100 and print A, B, C, D, or F. Add a clear message for values outside the range. Test boundary values such as 59, 60, 89, and 90." },
    { week: 3, title: "CS50P: loops, lists, and dictionaries", kind: "Course lesson", time: "55 min", icon: Video, sourceName: "CS50P · Week 2", sourceUrl: "https://cs50.harvard.edu/python/weeks/2/", detail: "Learn while, for, range, list, and dict. Pause after each new idea and write one five-line example before continuing." },
    { week: 3, title: "Repeat, collect, count", kind: "Practice", time: "40 min", icon: Terminal, sourceName: "Python tutorial · introduction", sourceUrl: "https://docs.python.org/3/tutorial/introduction.html", detail: "Read the official examples for strings and lists. Build a program that collects three study topics in a list, prints them with a loop, and reports the list length." },
    { week: 4, title: "Plan a focus-session calculator", kind: "Mini project", time: "30 min", icon: BookOpen, detail: "Before coding, write inputs, outputs, and three examples on paper. The program should convert completed 25-, 50-, or 90-minute sessions into total minutes and hours." },
    { week: 4, title: "Build and explain the calculator", kind: "Mini project", time: "60 min", icon: Code2, detail: "Implement the focus-session calculator using functions, conditionals, and a loop. Run at least five cases, then explain every line aloud. Save questions for the later AI tutor stage." },
  ], note: "Go and TypeScript wait until this Python month feels readable." },
  "Cloud & DevOps": { color: "cloud", icon: Cloud, label: "CLOUD & DEVOPS · MONTH 01", headline: "Linux to local cloud", description: "Learn the terminal first, understand cloud vocabulary, then practise AWS-shaped services locally with Floci.", current: "First-month route", stages: ["Linux navigation", "Files, pipes & permissions", "Cloud concepts", "Floci AWS practice"], credential: "Certification lane: AWS Cloud Practitioner is a later checkpoint. Do not book the exam during Month 1.", lessons: [
    { week: 1, title: "Ubuntu command line: first steps", kind: "Official tutorial", time: "45 min", icon: Terminal, sourceName: "Ubuntu · Command line for beginners", sourceUrl: "https://ubuntu.com/tutorials/command-line-for-beginners", detail: "Complete the overview, opening a terminal, pwd, cd, ls, mkdir, and file-path sections. Use a disposable practice folder and write down what each command changed." },
    { week: 1, title: "Terminal scavenger hunt", kind: "Local lab", time: "35 min", icon: Terminal, detail: "Create a folder tree for coding, cloud, and Japanese notes. Navigate into each folder using relative paths, return with .., and verify every location with pwd before moving." },
    { week: 2, title: "Files, pipes, and safe permissions", kind: "Official tutorial", time: "50 min", icon: BookOpen, sourceName: "Ubuntu · Command line for beginners", sourceUrl: "https://ubuntu.com/tutorials/command-line-for-beginners", detail: "Continue the Ubuntu tutorial through file manipulation, pipes, man pages, and administrator powers. Practise only inside your disposable folder; do not use sudo for the exercise." },
    { week: 2, title: "Explain client, server, and network", kind: "Concept check", time: "30 min", icon: Cloud, detail: "Draw one request from your browser to a server. Label client, DNS name, IP address, port, request, and response. Explain it in plain language without memorising vendor terms." },
    { week: 3, title: "AWS Cloud Practitioner Essentials: start", kind: "Official course", time: "60 min", icon: GraduationCap, sourceName: "AWS Skill Builder · Cloud Practitioner Essentials", sourceUrl: "https://skillbuilder.aws/learn/94T2BEN85A/aws-cloud-practitioner-essentials/8D79F3AVR7", detail: "Create a free Skill Builder account only if you want course tracking. Complete the opening cloud-concepts material and write one example each for compute, storage, database, and networking." },
    { week: 3, title: "Read the certification map", kind: "Certification prep", time: "25 min", icon: BookOpen, sourceName: "AWS · CLF-C02 exam guide", sourceUrl: "https://docs.aws.amazon.com/aws-certification/latest/cloud-practitioner-02/cloud-practitioner-02.html", detail: "Read the four exam domains and their weights. Treat this as a map, not a reason to rush the exam. Mark unfamiliar terms and return to the course." },
    { week: 4, title: "Start Floci and inspect the local cloud", kind: "Local cloud lab", time: "45 min", icon: Cloud, sourceName: "Floci · official quick start", sourceUrl: "https://floci.io/", detail: "Use the official quick start for your installed Floci setup. Start the AWS emulator, run its health or doctor check, and confirm the local endpoint. Keep all credentials as non-secret local test values." },
    { week: 4, title: "Create a local S3 bucket", kind: "Floci lab", time: "50 min", icon: Terminal, sourceName: "Floci · official repository", sourceUrl: "https://github.com/floci-io/floci", detail: "Point the AWS CLI at Floci, create one local S3 bucket, list it, add a small text file, and retrieve it. Write down which steps would involve cost or credentials on real AWS." },
  ], note: "Stay local this month. Real AWS begins after account, budget, IAM, and cleanup checks." },
  Japanese: { color: "japanese", icon: BookOpen, label: "JAPANESE · MONTH 01", headline: "Kana and first survival phrases", description: "Use short daily sessions. Learn to recognise, hear, and write kana while beginning useful spoken Japanese.", current: "First-month route", stages: ["Hiragana vowels & K/S/T/N", "Finish basic hiragana", "Dakuten & combinations", "Katakana introduction"], credential: "JLPT lane: this month builds pre-N5 reading habits. N2 remains the long-term direction.", lessons: [
    { week: 1, title: "Hiragana vowels: あいうえお", kind: "Kana lesson", time: "20 min daily", icon: Video, sourceName: "Japan Foundation · Memory Hint", sourceUrl: "https://marugoto.jpf.go.jp/en/e-learning/", detail: "Use HIRAGANA/KATAKANA Memory Hint. On day 1 learn the five vowels; on later days add one row at a time: K, S, T, then N. Say each sound, trace it, then recognise it without romaji." },
    { week: 1, title: "Daily five-minute blank recall", kind: "Recall", time: "5 min daily", icon: BookOpen, detail: "Close every chart. Write or name yesterday’s characters from memory, shuffle the order, and circle misses. Restudy only the misses before today’s new row." },
    { week: 2, title: "Finish basic hiragana rows", kind: "Kana lesson", time: "20 min daily", icon: Video, sourceName: "Japan Foundation · Memory Hint", sourceUrl: "https://marugoto.jpf.go.jp/en/e-learning/", detail: "Add H, M, Y, R, W, and ん across the week. Keep new material to one row per day and retain the five-minute recall before every session." },
    { week: 2, title: "Read kana inside greetings", kind: "Listening + reading", time: "25 min", icon: Headphones, sourceName: "Irodori Starter · Lesson 1", sourceUrl: "https://www.irodori.jpf.go.jp/en/starter/pdf.html", detail: "Open Starter Lesson 1, Good morning. Listen to the greeting activities, shadow each line, and underline kana you recognise. Understanding every grammar point is not required yet." },
    { week: 3, title: "Dakuten, handakuten, and small やゆよ", kind: "Kana lesson", time: "20 min daily", icon: BookOpen, sourceName: "Irodori · Kana resources", sourceUrl: "https://www.irodori.jpf.go.jp/en/resources.html", detail: "Study voiced sounds such as が and ぱ, then combinations such as きゃ. Compare each new form with its base character and read short pairs aloud." },
    { week: 3, title: "Ask for repetition", kind: "Listening + speaking", time: "30 min", icon: Headphones, sourceName: "Irodori Starter · Lesson 2", sourceUrl: "https://www.irodori.jpf.go.jp/en/starter/pdf.html", detail: "Use Starter Lesson 2. Practise もういちど、おねがいします and ゆっくり、おねがいします with the audio. Record yourself once, listen back, and repeat more slowly." },
    { week: 4, title: "Katakana vowels and K/S/T/N rows", kind: "Kana lesson", time: "20 min daily", icon: Video, sourceName: "Japan Foundation · Memory Hint", sourceUrl: "https://marugoto.jpf.go.jp/en/e-learning/", detail: "Begin katakana with the same rows you learned first in hiragana. Focus on look-alike pairs and your own name; do not abandon daily hiragana recall." },
    { week: 4, title: "Month-one kana checkpoint", kind: "Self-check", time: "30 min", icon: Check, detail: "Shuffle the basic hiragana, read twenty without romaji, write ten from sound, and note recurring misses. Success means a usable baseline, not perfect speed. Repeat weak rows next month." },
  ], note: "Daily consistency matters more than long cram sessions." },
};

export const curriculumTaskTitles = Object.fromEntries(
  Object.entries(paths).map(([name, path]) => [name, path.lessons.map(lesson => lesson.title)]),
) as Record<keyof typeof paths, string[]>;

export function LearningPath({ name, openLesson, completed, progress }: { name: keyof typeof paths; openLesson: (lesson: Lesson) => void; completed: string[]; progress: TrackProgress }) {
  const path = paths[name];
  const completedHere = path.lessons.filter(lesson => completed.includes(lesson.title)).length;
  return <div className={`learning-view ${path.color}`}><section className="path-intro"><div><span className="eyebrow"><path.icon size={17} /> {path.label}</span><h2>{path.headline}</h2><p>{path.description}</p><span className="path-note">{path.note}</span></div><div className="path-level"><span>TRACK LEVEL</span><strong>{String(progress.level).padStart(2, "0")}</strong><small>{progress.xp} XP · {progress.levelEnd - progress.xp} to next</small><Progress value={progress.percentToNext} aria-label={`${name} level ${progress.level}, ${progress.percentToNext} percent to next level`} /></div></section><div className="path-layout"><section className="lesson-list"><div className="section-heading"><h2>{path.current}</h2><span className="subtle">4 weeks · {path.lessons.length} tasks</span></div><p className="resource-note">Official sources were checked for Stage 4. Open a task for its exact assignment and source.</p>{path.lessons.map((lesson, i) => <button className="lesson-row" key={lesson.title} onClick={() => openLesson({ ...lesson, track: name })}><span className="lesson-index">{completed.includes(lesson.title) ? <Check size={17} /> : String(i + 1).padStart(2, "0")}</span><span className="lesson-row-content"><span className="week-label">WEEK {lesson.week}</span><strong>{lesson.title}</strong><span><lesson.icon size={13} /> {lesson.kind}<i />{lesson.time}{lesson.sourceUrl && <><i />Reviewed source</>}{completed.includes(lesson.title) && " · Saved attempt"}</span></span><ArrowRight size={19} /></button>)}</section><aside className="path-map"><span className="eyebrow">4-WEEK MAP</span><ol>{path.stages.map((stage, i) => <li key={stage} className={i === 0 ? "current-stage" : ""}><span className="path-node">{i === 0 ? <Play size={10} fill="currentColor" /> : String(i + 1)}</span><div><span>WEEK 0{i + 1}</span><strong>{stage}</strong></div></li>)}</ol><div className="credential-note"><GraduationCap size={16} /><p>{path.credential}</p></div></aside></div></div>;
}

const week: { day: Weekday; name: string; note: string }[] = [
  { day: "Mon", name: "Monday", note: "Campus supervision may move this day." },
  { day: "Tue", name: "Tuesday", note: "Campus · 09:00–15:00" },
  { day: "Wed", name: "Wednesday", note: "Campus · 13:00–17:00" },
  { day: "Thu", name: "Thursday", note: "Open study day" },
  { day: "Fri", name: "Friday", note: "Open study day" },
  { day: "Sat", name: "Saturday", note: "Build and connect ideas" },
  { day: "Sun", name: "Sunday", note: "Review, catch up, or rest" },
];
const kindLabels: Record<SessionKind, string> = { coding: "Coding", cloud: "Cloud & DevOps", japanese: "Japanese", campus: "Campus", personal: "Personal", review: "Weekly review" };
const kindIcons = { coding: Code2, cloud: Cloud, japanese: BookOpen, campus: GraduationCap, personal: Leaf, review: Check };

export function ScheduleView({ sessions, onSave, onDelete }: { sessions: PlannedSession[]; onSave: (session: PlannedSession) => void; onDelete: (id: string) => void }) {
  const [day, setDay] = useState<Weekday>("Sat");
  const [editing, setEditing] = useState<PlannedSession | null>(null);
  function startNew() { setEditing({ id: createRecordId("plan"), day, time: "10:00", durationMinutes: 25, title: "New study session", kind: "coding" }); }
  return <section className="schedule-view"><div className="schedule-title"><div><span className="eyebrow">YOUR WEEK</span><h2>Weekly schedule</h2><p>Move sessions when campus, gym, or your energy changes.</p></div><div className="schedule-tools"><span className="timezone"><Clock3 size={15} /> Jakarta · WIB</span><button className="secondary-btn" onClick={startNew}><Plus size={16} /> Add session</button></div></div><Tabs value={day} onValueChange={value => setDay(value as Weekday)}><TabsList className="week-tabs">{week.map(d => <TabsTrigger value={d.day} key={d.day}><span>{d.day}</span><span className="week-dot" /></TabsTrigger>)}</TabsList>{week.map(d => { const daySessions = sessions.filter(session => session.day === d.day).sort((a, b) => a.time.localeCompare(b.time)); return <TabsContent value={d.day} key={d.day}><div className="day-plan"><div className="day-plan-heading"><h3>{d.name}</h3><span>{d.note} · {daySessions.length} sessions</span></div>{daySessions.length ? daySessions.map(session => { const Icon = kindIcons[session.kind]; return <button className={`schedule-event ${session.kind}`} key={session.id} onClick={() => setEditing(session)}><span className="event-time">{session.time}</span><div><span className="eyebrow">{kindLabels[session.kind]}</span><strong>{session.title}</strong><p>{session.durationMinutes} minutes · select to move or edit</p></div><Icon size={20} /><Pencil size={15} /></button>; }) : <div className="empty-day"><strong>No sessions yet</strong><p>Leave the day open or add a study block.</p><button className="text-btn" onClick={startNew}><Plus size={15} /> Add a session</button></div>}</div></TabsContent>; })}</Tabs><p className="preview-footnote">Changes save immediately on this device. Select any session to change its day, time, duration, title, or type.</p><Dialog open={!!editing} onOpenChange={open => { if (!open) setEditing(null); }}><DialogContent className="schedule-editor"><DialogHeader><span className="eyebrow"><Pencil size={14} /> PLAN SESSION</span><DialogTitle>{sessions.some(session => session.id === editing?.id) ? "Edit session" : "Add session"}</DialogTitle><DialogDescription>Move this block to another day or adjust it around campus and gym.</DialogDescription></DialogHeader>{editing && <div className="session-form"><label><span>Title</span><input value={editing.title} maxLength={120} onChange={event => setEditing({ ...editing, title: event.target.value })} /></label><div className="session-form-row"><label><span>Day</span><select value={editing.day} onChange={event => setEditing({ ...editing, day: event.target.value as Weekday })}>{week.map(item => <option value={item.day} key={item.day}>{item.name}</option>)}</select></label><label><span>Time</span><input type="time" value={editing.time} onChange={event => setEditing({ ...editing, time: event.target.value })} /></label></div><div className="session-form-row"><label><span>Type</span><select value={editing.kind} onChange={event => setEditing({ ...editing, kind: event.target.value as SessionKind })}>{Object.entries(kindLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label><label><span>Minutes</span><input type="number" min="5" max="720" step="5" value={editing.durationMinutes} onChange={event => setEditing({ ...editing, durationMinutes: Number(event.target.value) })} /></label></div><div className="session-editor-actions">{sessions.some(session => session.id === editing.id) && <button className="danger-btn" onClick={() => { onDelete(editing.id); setEditing(null); }}><Trash2 size={16} /> Remove</button>}<button className="primary-btn" onClick={() => { onSave({ ...editing, title: editing.title.trim() || "Study session", durationMinutes: Math.max(5, Math.min(720, editing.durationMinutes || 25)) }); setDay(editing.day); setEditing(null); }}><Check size={16} /> Save session</button></div></div>}</DialogContent></Dialog></section>;
}

const evidenceLabels = { introduced: "Introduced", helped: "With help", independent: "Independent", remembered: "Remembered later" } as const;

export function ProgressView({ completed, attemptCount, focusMinutes, snapshot }: { completed: string[]; attemptCount: number; focusMinutes: number; snapshot: ProgressSnapshot }) {
  const focusLabel = focusMinutes >= 60 ? (focusMinutes / 60).toFixed(focusMinutes % 60 === 0 ? 0 : 1) : focusMinutes;
  const focusUnit = focusMinutes >= 60 ? "hours" : "minutes";
  const proven = snapshot.topics.filter(topic => topic.state === "independent" || topic.state === "remembered").length;
  const maxDay = Math.max(1, ...snapshot.weekly.map(day => day.xp + day.focusMinutes));
  return <section className="progress-view"><div className="progress-intro"><span className="eyebrow">STAGE 9 · PROGRESS</span><h2>Your work, with receipts.</h2><p>XP marks unique evidence. Repeating the same action keeps its history, but never awards the same credit twice.</p></div><div className="progress-summary"><div><span>Total track XP</span><strong>{snapshot.totalXp} <small>XP</small></strong><p>{snapshot.creditedActions} unique awards across three tracks.</p></div><div><span>Proven topics</span><strong>{proven} <small>topics</small></strong><p>Independent checks or later recall; lesson clicks alone do not count.</p></div><div><span>Saved focus time</span><strong>{focusLabel} <small>{focusUnit}</small></strong><p>{attemptCount} attempts · {completed.length} unique lessons tried.</p></div></div>
  <section className="weekly-progress" aria-labelledby="weekly-progress-title"><div className="section-heading"><div><span className="eyebrow">THIS WEEK · {snapshot.weekStartLabel}</span><h2 id="weekly-progress-title">Study rhythm</h2></div><span className="subtle">XP + finished focus time</span></div><div className="week-chart">{snapshot.weekly.map(day => <div className="week-column" key={day.key}><div className="week-values"><span>{day.xp ? `+${day.xp} XP` : "—"}</span><strong>{day.focusMinutes ? `${day.focusMinutes}m` : "0m"}</strong></div><div className="week-bar"><span style={{ height: `${Math.max(day.xp || day.focusMinutes ? 8 : 0, Math.round(((day.xp + day.focusMinutes) / maxDay) * 100))}%` }} /></div><div className="week-day"><strong>{day.label}</strong><span>{day.dateLabel}</span></div></div>)}</div></section>
  <div className="section-heading progress-track-heading"><h2>Separate track levels</h2><span className="subtle">Level 2 starts at 100 XP</span></div><div className="progress-tracks">{Object.entries(paths).map(([name, path]) => { const progress = snapshot.tracks[name as TrackName]; return <div className={`progress-track ${path.color}`} key={name}><path.icon size={22} /><div><strong>{name}</strong><p>{progress.xp} XP · {progress.levelEnd - progress.xp} XP to level {progress.level + 1}</p><Progress value={progress.percentToNext} aria-label={`${name} level ${progress.level}, ${progress.percentToNext} percent to next level`} /></div><span>LEVEL {String(progress.level).padStart(2, "0")}<br /><strong>{progress.percentToNext}% to next</strong></span></div>; })}</div>
  <section className="evidence-board"><div className="section-heading"><div><span className="eyebrow">TOPIC EVIDENCE</span><h2>What the level is built from</h2></div><span className="subtle">Strongest saved state per topic</span></div>{snapshot.topics.length ? <div className="evidence-list">{snapshot.topics.slice(0, 12).map(topic => <div className="evidence-row" key={topic.key}><span className={`evidence-state ${topic.state}`}>{evidenceLabels[topic.state]}</span><div><strong>{topic.topic}</strong><p>{topic.track} · {topic.detail}</p></div><time dateTime={topic.updatedAt}>{validShortDate(topic.updatedAt)}</time></div>)}</div> : <div className="progress-empty"><strong>No evidence yet</strong><p>Open a lesson, run a Python check, practise a Japanese card, or start a cloud lab.</p></div>}</section>
  <div className="mastery-explainer"><span className="eyebrow">HOW CREDIT WORKS</span><h3>Four evidence states, one honest direction</h3><div className="mastery-steps">{["Introduced", "With help", "Independent", "Remembered later"].map((label, i) => <span key={label}><b>0{i + 1}</b>{label}{i < 3 && <ArrowRight size={16} />}</span>)}</div><p>A unique lesson attempt earns 10 XP. A first Python pass earns 40 XP. A first correct Japanese recall earns 15 XP, then a correct recall at least 20 hours later earns 25 more. Cloud labs earn 5 XP when started, 25 with guided evidence, or 35 when a narrow local check verifies them. Each award key is counted once.</p></div></section>;
}

function validShortDate(value: string) {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? "Saved" : date.toLocaleDateString("en-US", { month: "short", day: "numeric" });
}

